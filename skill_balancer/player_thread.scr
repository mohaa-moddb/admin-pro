main:
	if(level.run[team_balance] != 1){end}

	if (self.runstage == "0" || self.runstage == "1")
	{
		end
	}
	else
	{
		self.runstage = "0"
		self.ic = int 1
		self.movedrecent = "0"
		self.isrunningc = "0"
		exec skill_balancer\move_count.scr
		waitframe
		end
	}
end

